{
  "name": "js-brute-attack",
  "scripts": {
    "compile": "tsc",
    "server": "node server.js",
    "start": "npm-run-all --parallel \"compile -- --watch\" server",
    "clean": "rimraf dist/",
    "build": "npm-run-all clean compile"
  },
  "devDependencies": {
    "express": "^4.16.4",
    "npm-run-all": "^4.1.3",
    "rimraf": "^2.6.2",
    "typescript": "^3.1.6"
  }
}
